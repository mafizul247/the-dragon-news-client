import React from 'react';
import useTitle from '../../hooks/useTitle';

const About = () => {
    useTitle('About')
    return (
        <div>
            <h3>About</h3>
        </div>
    );
};

export default About;